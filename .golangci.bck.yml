# golangci-lint v1.x compatible configuration
# Removed 'version: 2' for v1 compatibility

linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: true
  goconst:
    min-len: 3
    min-occurrences: 3

  gocyclo:
    min-complexity: 15
  misspell:
    locale: US
  revive:
    rules:
      - name: exported
        severity: warning
      - name: error-return
        severity: warning

linters:
  enable:
    - errcheck
    - goconst
    - gosec
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unconvert
    - unparam
    - unused

issues:
  exclude-rules:
    - path: _test.go
      linters:
        - errcheck
        - goconst
        - gosec
        - revive
        - staticcheck
  max-issues-per-linter: 0
  max-same-issues: 0
