provider_installation {
  dev_overrides {
    "registry.terraform.io/davidshato/httpx" = "../../"
  }

  # For all other providers, install them directly from their default registry
  # if not overridden above.
  direct {}
}

